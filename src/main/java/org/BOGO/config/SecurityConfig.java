package org.BOGO.config;


/**
 * Security configuration — JWT filters, role-based access rules,
 * and CORS settings go here.
 */
public class SecurityConfig {
    // TODO: configure HttpSecurity, JwtFilter, CORS, etc.
}
